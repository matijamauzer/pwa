<?php
$naslov     = "PHP dokument";
$ime        = "Matija Mauzer";
$url        = "https://hr.wikipedia.org/wiki/PHP";
$urlNaziv   = "Saznaj više o PHP-u";
?>
<!DOCTYPE html>
<html lang="hr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><?php echo htmlspecialchars($naslov); ?></title>
  <meta name="description" content="Prvi PHP dokument s osnovnim CSS stilom.">
  <style>
    :root { --pozadina:#0f172a; --blok:#ffffff; --text:#111827; --muted:#6b7280; --naglasak:#2563eb; }
    * { box-sizing: border-box; }
    body { margin:0; font-family: system-ui,-apple-system,"Segoe UI",Roboto,sans-serif;
           background:var(--pozadina); color:var(--text); }
    .sadrzaj { max-width:720px; margin:48px auto; background:var(--blok); padding:32px;
            border-radius:16px; box-shadow:0 10px 30px rgba(0,0,0,.08); }
    h1 { margin:0 0 12px; font-size:2rem; }
    p  { margin:0 0 14px; line-height:1.6; }
    .gumb { display:inline-block; padding:10px 16px; border:1px solid var(--naglasak);
           border-radius:10px; text-decoration:none; }
    .gumb:hover { background:var(--naglasak); color:#fff; }
    footer { margin-top:20px; font-size:.9rem; color:var(--muted); }
  </style>
</head>
<body>
  <main class="sadrzaj">
    <h1><?php echo htmlspecialchars($naslov); ?></h1>
    <p>Ovu stranicu izradio/la je <strong><?php echo htmlspecialchars($ime); ?></strong>.</p>
    <p>PHP je serverski jezik koji generira HTML ili JSON odgovor prema klijentu.</p>
    <p>
      <a class="gumb" href="<?php echo htmlspecialchars($url); ?>"
         target="_blank" rel="noopener"><?php echo htmlspecialchars($urlNaziv); ?></a>
    </p>
  </main>
</body>
</html>
